#ifndef DECLARE_H
#define DECLARE_H

#include <raylib.h>

typedef enum GameScreen {MAINMENU, LINKEDLIST, HASH, TREE, GRAPH} GameScreen;

const int screenWidth = 1000;
const int screenHeight = 800;

bool InsertInput = false;

#endif
